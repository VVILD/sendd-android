<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    android:background="@drawable/nav_drawer_button_background">

    <ImageView
        android:id="@+id/nav_drawer_item_icon"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:layout_centerVertical="true"
        android:layout_marginLeft="16dp"
        android:layout_marginStart="16dp"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/nav_drawer_item_title"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginLeft="16dp"
        android:layout_marginStart="16dp"
        android:layout_toEndOf="@+id/nav_drawer_item_icon"
        android:layout_toRightOf="@+id/nav_drawer_item_icon"
        android:gravity="center_vertical"
        android:text="Dradwer Item Title"
        android:textColor="@android:color/black"
        android:textSize="16sp" />

</RelativeLayout>